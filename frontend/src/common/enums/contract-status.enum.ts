export enum ContractStatus {
  ACTIVE = 'active',
  CLOSED = 'closed',
  NOT_STARTED = 'not_started',
  VALIDATE_ONG = 'validate_ong',
  VALIDATE_VOLUNTEER = 'validate_volunteer',
  REJECTED = 'rejected',
}
